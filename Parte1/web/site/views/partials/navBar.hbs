<nav class="navbar navbar-expand-lg bg-light">
  <div class="container-fluid">

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link
          {{#if home}}
            active disabled" 
          {{/if}}
          " aria-current="page" href="/">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link 
          {{#if groups}}
            active disabled" 
          {{/if}}
          aria-current="page" href="/groups">Groups</a>
        </li>
        <li class="nav-item">
          <a class="nav-link
          {{#if createGroup}}
            active disabled" 
          {{/if}}
          " href="/groups/newGroup">CreateGroup</a>
        </li>
          {{#if user}}
          {{else}}
              <li class="nav-item">
              <a class="nav-link
              {{#if signup}}
                active disabled"
              {{/if}}
                 aria-current="page"  href="/signup">Signup</a>
          {{/if}}
          </li>
      </ul>
    </div>
      {{> logout}}
  </div>
</nav>